<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?android:attr/selectableItemBackground"
    android:padding="6dp">

    <ImageView
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:id="@+id/usuarios_adatpter_cpver"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:src="@drawable/ic_person_black_24dp" />

    <TextView
        android:layout_width="match_parent"
        android:maxLines="2"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:text="Medium Text"
        android:id="@+id/usuarios_adapter_titulo"
        android:layout_alignTop="@+id/usuarios_adatpter_cpver"
        android:layout_toRightOf="@+id/usuarios_adatpter_cpver"
        android:layout_toEndOf="@+id/usuarios_adatpter_cpver"
        android:layout_marginLeft="6dp"
        android:layout_alignBottom="@+id/usuarios_adatpter_cpver"
        android:gravity="center_vertical" />

</RelativeLayout>